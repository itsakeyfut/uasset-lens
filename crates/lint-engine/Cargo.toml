[package]
name    = "lint-engine"
version.workspace = true
edition.workspace = true

[dependencies]
asset-db    = { path = "../asset-db" }
bp-analyzer = { path = "../bp-analyzer" }
scanner     = { path = "../scanner" }
shared      = { path = "../shared" }
