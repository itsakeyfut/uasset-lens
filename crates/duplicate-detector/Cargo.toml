[package]
name    = "duplicate-detector"
publish = false
version.workspace = true
edition.workspace = true

[dependencies]
asset-db = { path = "../asset-db" }
shared   = { path = "../shared" }

[dev-dependencies]
asset-db = { path = "../asset-db", features = ["test-support"] }
