[package]
name    = "bp-analyzer"
publish = false
version.workspace = true
edition.workspace = true

[dependencies]
scanner = { path = "../scanner" }

[dev-dependencies]
shared  = { path = "../shared" }
scanner = { path = "../scanner", features = ["test-support"] }
