[workspace]
resolver = "2"
members = [
    "crates/uasset-lens-shared",
    "crates/uasset-lens-scanner",
    "crates/uasset-lens-asset-db",
    "crates/uasset-lens-dependency-graph",
    "crates/uasset-lens-analysis",
    "crates/uasset-lens-watcher",
    "crates/uasset-lens-git-diff",
    "apps/cli",
]

[workspace.package]
version      = "0.1.0"
edition      = "2024"
rust-version = "1.96.0"
authors      = ["itsakeyfut"]
license      = "MIT OR Apache-2.0"
repository   = "https://github.com/itsakeyfut/uasset-lens"
homepage     = "https://github.com/itsakeyfut/uasset-lens"
description  = "Fast, CLI-first static analyzer for Unreal Engine 5 .uasset / .umap files"
keywords     = ["unreal-engine", "uasset", "static-analysis", "game-development"]
categories   = ["development-tools", "command-line-utilities"]

[workspace.dependencies]
# Internal crates (path + version required for crates.io publishing)
uasset-lens-shared              = { path = "crates/uasset-lens-shared",              version = "0.1.0" }
uasset-lens-scanner             = { path = "crates/uasset-lens-scanner",             version = "0.1.0" }
uasset-lens-asset-db            = { path = "crates/uasset-lens-asset-db",            version = "0.1.0" }
uasset-lens-dependency-graph    = { path = "crates/uasset-lens-dependency-graph",    version = "0.1.0" }
uasset-lens-analysis            = { path = "crates/uasset-lens-analysis",            version = "0.1.0" }
uasset-lens-watcher             = { path = "crates/uasset-lens-watcher",             version = "0.1.0" }
uasset-lens-git-diff            = { path = "crates/uasset-lens-git-diff",            version = "0.1.0" }

thiserror  = "2.0.18"
serde      = { version = "1.0.228", features = ["derive"] }
byteorder  = "1.5.0"
rayon      = "1.12.0"
walkdir    = "2.5.0"
rusqlite   = { version = "0.39.0", features = ["bundled"] }
clap       = { version = "4.6.1", features = ["derive"] }
anyhow     = "1.0.102"
tracing    = "0.1.44"
petgraph   = "0.8.3"
serde_json = "1.0.149"
toml       = "1"
globset    = "0.4"
notify     = "6"
tempfile   = "3.27.0"
ctrlc      = "3.5.2"

[profile.dev]
opt-level    = 0
debug        = true
incremental  = true

# Optimize dependencies (rusqlite, rayon, byteorder, etc.) even in dev builds so
# binary parsing and SQLite operations don't become the bottleneck during iteration.
[profile.dev.package."*"]
opt-level = 3

[profile.release]
opt-level = 3
lto       = "thin"
strip     = true

[workspace.lints.rust]
unsafe_code = "warn"

[workspace.lints.clippy]
all                              = { level = "warn", priority = -1 }
pedantic                         = { level = "warn", priority = -1 }
unwrap_used                      = "warn"
expect_used                      = "warn"
panic                            = "warn"
module_name_repetitions          = "allow"
too_many_lines                   = "allow"
must_use_candidate               = "allow"
missing_errors_doc               = "allow"
missing_panics_doc               = "allow"
similar_names                    = "allow"
if_not_else                      = "allow"
match_same_arms                  = "allow"
uninlined_format_args            = "allow"
redundant_closure_for_method_calls = "allow"
